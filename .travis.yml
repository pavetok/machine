---
language: python
python: "2.7"

addons:
  apt:
    packages:
    - python-pip

install:
  - whoami
  - pip install -r requirements.txt

script:
  - make lint
  - ansible-playbook tests/machine.yml -i tests/inventory

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
